version: '3.8'

services:
  anschreiben-generator:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - ../outputs:/app/outputs
    restart: unless-stopped